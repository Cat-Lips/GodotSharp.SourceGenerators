using System;

namespace Godot;

static partial class {{ClassName}}
{
{{~ for autoload in Autoloads ~}}
    /// <summary>Autoload: {{autoload.GodotName}}</summary>
    public static {{autoload.Type}} {{autoload.DisplayName}} => field ??= root?.GetNodeOrNull<{{autoload.Type}}>("{{autoload.GodotName}}");
{{~ end ~}}
}
